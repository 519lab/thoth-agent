# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader
src/data/skills.json
# Note: static/llms.txt + static/llms-full.txt are intentionally committed
# (regenerate-and-commit policy) so the served files are the source of truth,
# PR diffs are honest, and CI builds are reproducible. Regenerate via
# `python3 website/scripts/generate-llms-txt.py` and commit the result.

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
